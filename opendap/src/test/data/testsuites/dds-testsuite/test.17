#
# Test error messages; When `Maps' is misspelled.

Dataset {
    Grid {
      Array:
        Byte temp[100][7];
      Map:
        Float64 steps[100];
	String colors[7];
    } oddTemp;
} data2;
