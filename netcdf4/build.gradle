jar {
    manifest {
        attributes 'Implementation-Title': 'netcdf4'
    }
}

dependencies {
    compile project(':cdm')
    compile libraries["jna"]
    compile libraries["slf4j-api"]
    testRuntime libraries["slf4j-jdk14"]
}
