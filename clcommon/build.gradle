jar {
    manifest {
        attributes 'Implementation-Title': 'clcommon'
    }
}

/* sourceSets {
    main {
        java {
            exclude 'thredds/inventory/bdb/*.java'
        }
    }
} */

dependencies {
  compile project(':cdm')
  compile 'com.sleepycat:je:4.0.92'
  testCompile project(':cdm').sourceSets.test.output
}