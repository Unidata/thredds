dependencies {
    // A lot of these depedencies should be in the testRuntime config, not testCompile.

    testCompile project(":cdm")
    testCompile project(":clcommon")
    testCompile project(":grib")
    testCompile project(":opendap")
    testCompile project(":visad")

    testCompile libraries["visad"]
    testCompile libraries["protobuf-java"]
    testCompile libraries["jdom2"]
    testRuntime libraries["jaxen"]
    testCompile libraries["commons-io"]

    testCompile libraries["spring-test"]
    testCompile libraries["com.eclipsesource.restfuse"]
    // TODO: testCompile cdm:test-jar

    testCompile libraries["slf4j-api"]
    testRuntime libraries["slf4j-jdk14"]
    testRuntime libraries["jcl-over-slf4j"]
}
